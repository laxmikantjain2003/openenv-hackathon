[project]
name = "api-debugger-submission"
version = "0.1.0"
description = "Hackathon submission for OpenEnv"
requires-python = ">=3.10"
dependencies = [
    "openai",
    "pydantic",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"